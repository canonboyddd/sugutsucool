const i=document.getElementById('input'),o=document.getElementById('output'),s=document.getElementById('status');document.getElementById('run').onclick=()=>{let a=i.value.split(/\r?\n/);if(document.getElementById('blank').value==='remove')a=a.filter(x=>x.trim());a.sort((x,y)=>x.localeCompare(y,'ja',{numeric:true,sensitivity:'base'}));if(document.getElementById('order').value==='desc')a.reverse();o.value=a.join('\n');s.textContent=`${a.length}行を並び替えました。`;s.className='status success'};document.getElementById('copy').onclick=async()=>{await copyText(o.value);s.textContent='コピーしました。'};