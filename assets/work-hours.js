const result=document.getElementById('result'),s=document.getElementById('status');function mins(v){const [h,m]=v.split(':').map(Number);return h*60+m}function fmt(m){return `${Math.floor(m/60)}時間${m%60}分`}document.getElementById('run').onclick=()=>{const a=document.getElementById('start').value,b=document.getElementById('end').value,br=Math.max(0,+document.getElementById('break').value||0);if(!a||!b)return;let gross=mins(b)-mins(a);if(gross<0)gross+=1440;const net=gross-br;if(net<0){s.textContent='休憩時間が拘束時間を超えています。';s.className='status danger';result.classList.remove('show');return}document.getElementById('gross').textContent=fmt(gross);document.getElementById('rest').textContent=fmt(br);document.getElementById('net').textContent=fmt(net);result.classList.add('show');s.textContent='';};