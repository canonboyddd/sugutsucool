let file=null,out=null;const run=document.getElementById('run'),statusEl=document.getElementById('status'),result=document.getElementById('result'),canvas=document.getElementById('preview');setupDropzone({zoneId:'drop',inputId:'file',multiple:false,onFiles:f=>{file=f[0];run.disabled=false;statusEl.textContent=`選択中: ${file.name}`;result.classList.remove('show')}});run.onclick=async()=>{try{const img=await imageFromFile(file),deg=+document.getElementById('rotate').value,flip=document.getElementById('flip').value,swap=deg===90||deg===270;canvas.width=swap?img.naturalHeight:img.naturalWidth;canvas.height=swap?img.naturalWidth:img.naturalHeight;const ctx=canvas.getContext('2d');ctx.save();ctx.translate(canvas.width/2,canvas.height/2);ctx.rotate(deg*Math.PI/180);ctx.scale(flip==='horizontal'?-1:1,flip==='vertical'?-1:1);ctx.drawImage(img,-img.naturalWidth/2,-img.naturalHeight/2);ctx.restore();out=await canvasToBlob(canvas,'image/png');result.classList.add('show');statusEl.textContent='変換しました。';statusEl.className='status success'}catch(e){statusEl.textContent='処理できませんでした。';statusEl.className='status danger'}};document.getElementById('download').onclick=()=>out&&downloadBlob(out,(file.name.replace(/\.[^.]+$/,'')||'image')+'-edited.png');