(function(){const y=document.querySelector('[data-year]');if(y)y.textContent=new Date().getFullYear();const search=document.querySelector('#toolSearch');const empty=document.querySelector('#emptySearch');if(search){const cards=[...document.querySelectorAll('[data-tool]')];search.addEventListener('input',()=>{const q=search.value.trim().toLowerCase().replace(/\s+/g,' ');let count=0;cards.forEach(card=>{const hay=(card.dataset.tool+' '+card.textContent).toLowerCase();const show=!q||q.split(' ').every(w=>hay.includes(w));card.style.display=show?'block':'none';if(show)count++});if(empty)empty.style.display=count?'none':'block';});}})();
function fmtBytes(n){if(n<1024)return n+' B';if(n<1048576)return (n/1024).toFixed(1)+' KB';return (n/1048576).toFixed(2)+' MB'}
function downloadBlob(blob,name){const a=document.createElement('a');a.href=URL.createObjectURL(blob);a.download=name;document.body.appendChild(a);a.click();setTimeout(()=>{URL.revokeObjectURL(a.href);a.remove()},1500)}
function setupDropzone({zoneId,inputId,onFiles,multiple=true}){const z=document.getElementById(zoneId),i=document.getElementById(inputId);if(!z||!i)return;z.addEventListener('click',()=>i.click());z.addEventListener('keydown',e=>{if(e.key==='Enter'||e.key===' '){e.preventDefault();i.click()}});['dragenter','dragover'].forEach(ev=>z.addEventListener(ev,e=>{e.preventDefault();z.classList.add('drag')}));['dragleave','drop'].forEach(ev=>z.addEventListener(ev,e=>{e.preventDefault();z.classList.remove('drag')}));z.addEventListener('drop',e=>{const fs=[...e.dataTransfer.files];if(fs.length)onFiles(multiple?fs:[fs[0]])});i.addEventListener('change',()=>{const fs=[...i.files];if(fs.length)onFiles(multiple?fs:[fs[0]])});}
async function imageFromFile(file){return await new Promise((res,rej)=>{const img=new Image();const url=URL.createObjectURL(file);img.onload=()=>{URL.revokeObjectURL(url);res(img)};img.onerror=e=>{URL.revokeObjectURL(url);rej(e)};img.src=url})}
function canvasToBlob(canvas,type='image/png',quality=.92){return new Promise((res,rej)=>canvas.toBlob(b=>b?res(b):rej(new Error('blob')),type,quality))}
function copyText(text){if(navigator.clipboard&&window.isSecureContext)return navigator.clipboard.writeText(text);const t=document.createElement('textarea');t.value=text;document.body.appendChild(t);t.select();document.execCommand('copy');t.remove();return Promise.resolve()}
