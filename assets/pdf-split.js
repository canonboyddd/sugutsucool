function parsePageSpec(s,max){const set=new Set();for(const part of s.split(',').map(x=>x.trim()).filter(Boolean)){if(part.includes('-')){let[a,b]=part.split('-').map(Number);if(!Number.isInteger(a)||!Number.isInteger(b))continue;if(a>b)[a,b]=[b,a];for(let i=a;i<=b;i++)if(i>=1&&i<=max)set.add(i-1)}else{const n=Number(part);if(Number.isInteger(n)&&n>=1&&n<=max)set.add(n-1)}}return [...set].sort((a,b)=>a-b)}let file=null;const run=document.getElementById('run'),statusEl=document.getElementById('status');setupDropzone({zoneId:'drop',inputId:'file',multiple:false,onFiles:f=>{file=f[0];run.disabled=false;statusEl.textContent=`選択中: ${file.name} (${fmtBytes(file.size)})`;}});run.onclick=async()=>{run.disabled=true;statusEl.textContent='処理中…';try{const src=await PDFLib.PDFDocument.load(await file.arrayBuffer()),idx=parsePageSpec(document.getElementById('pages').value,src.getPageCount());if(!idx.length)throw new Error('pages');const out=await PDFLib.PDFDocument.create(),pages=await out.copyPages(src,idx);pages.forEach(p=>out.addPage(p));const bytes=await out.save();downloadBlob(new Blob([bytes],{type:'application/pdf'}),(file.name.replace(/\.pdf$/i,'')||'pdf')+'-pages.pdf');statusEl.textContent=`${idx.length}ページを抽出しました。`;statusEl.className='status success'}catch(e){statusEl.textContent='ページ指定を確認してください。例：1,3,5-8';statusEl.className='status danger'}finally{run.disabled=false}};